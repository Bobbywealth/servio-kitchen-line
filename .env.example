# Servio Kitchen Line — environment variables

# Database (shared with main Servio platform — Render Postgres)
DATABASE_URL=postgresql://user:password@host:5432/dbname
DATABASE_SSL=true

# Server
PORT=5001

# Optional: restrict CORS to Servio domains
# CORS_ORIGIN=https://servio-worker.onrender.com
